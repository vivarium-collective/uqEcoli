Wrappers API
============

.. caution::
   The ``uq.wrappers`` module (``SimulationWrapper``, ``PrecomputedWrapper``,
   etc.) has been replaced by the subprocess-based ``TimeseriesGeneratorVecoli``
   (``uq/generators/vecoli.py``) and the SMS-API client (``uq/remote.py``).

   For the current workflow, see:

   * :doc:`../getting_started` — sample + quantify pipeline
   * :doc:`../cli_reference` — all CLI commands
   * ``uq/remote.py`` — ``SmsApiClient`` for remote execution
